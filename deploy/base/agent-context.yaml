apiVersion: apps/v1
kind: Deployment
metadata: 
  name: agent-context
  namespace: incident-copilot
spec:
  replicas: 1
  selector: 
    matchLabels: 
      app: agent-context
  template:
    metadata: 
      labels: 
        app: agent-context
    spec:
      containers:
      - name: app
        image: docker.io/petitsinge/incident-copilot-agent-context:latest
        ports: 
        - containerPort: 8000
        envFrom:
        - secretRef: 
            name: copilot-secrets
        - configMapRef: 
            name: copilot-config
---
apiVersion: v1
kind: Service
metadata: 
  name: agent-context
  namespace: incident-copilot
spec:
  selector: 
    app: agent-context
  ports: 
  - port: 8000
    targetPort: 8000